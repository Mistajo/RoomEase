<nav class="navbar navbar-expand-lg bg-light shadow">
	<div class="container-fluid">
		<a class="navbar-brand col-md-3 col-lg-2 my-1  fs-6" href="{{path('visitor.welcome.index')}}"><img src="{{asset('Logo/RoomEase_Logo.png')}}" alt="RoomEase Logo" class=' w-50 h-25'></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<form class="d-flex me-auto" role="search">
				<input class="form-control me-2" type="search" placeholder="Rechercher" aria-label="Search">
				<button class="btn btn-outline-success" type="submit">Rechercher</button>
			</form>
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link" href="{{path('visitor.welcome.index')}}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('admin.equipments.index')}}">Equipements</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('admin.meetingroom.index')}}">Salles de Reunions</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('admin.user.index')}}">Liste Des Utilisateurs</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('admin.reservation.index')}}">Liste Des Reservations</a>

				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('visitor.contact.create')}}">Contact</a>
				</li>
			</ul>
			<div class="ms-auto">
				{% if app.user %}
					<ul class="navbar-nav">
						<li class="nav-item dropdown">
							<a href="" class="nav-link dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false" data-bs-display="static">
								<i class="fa-solid fa-user-secret">
									Bonjour
									{{app.user.firstName}}</i>
							</a>
							<ul class="dropdown-menu dropdown-menu-end">
								<li>
									<a class="dropdown-item" href="{{path('admin.profile.index')}}">Mon Profil</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{path('user.home.index')}}">Espace client</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{path('admin.setting.index')}}">Paramètre du Site</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{path('app_logout')}}">Déconnexion</a>
								</li>
							</ul>
						</li>
					</ul>
				{% else %}
					<a href="{{path('visitor.authentication.login')}}" class="btn btn-primary my-3 my-lg-0">Connexion</a>
					<a href="{{path('visitor.registration.register')}}" class="btn btn-light my-3 my-lg-0 mx-lg-2">Inscription</a>
				{% endif %}

			</div>

		</div>
	</div>
</nav>
